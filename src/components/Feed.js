const Feed = {
    render: () => {
        const feed = document.createElement("div");
        feed.id = "feed";
        feed.className = "container-md justify-content-center";
        return feed;
    },
};
