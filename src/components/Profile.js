const Profile = {
    render: () => {
        const profile = document.createElement("div");
        profile.id = "profile";
        profile.className = "container-md justify-content-center feed";
        return profile;
    },
};
